import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 55,
    width: '100%',
    borderRadius: 5,
    marginTop: 8,
    justifyContent: 'center',
    alignItems: 'center',
  },

  buttonText: {
    fontSize: 14,
    fontFamily: 'Bold',
    color: '#f2f2f2',
  },
});
