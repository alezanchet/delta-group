import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    marginBottom: 32,
  },

  title: {
    fontSize: 16,
    fontFamily: 'Bold',
    color: '#333333',
    marginBottom: 8,
    marginTop: 16,
  },

  subtitle: {
    fontSize: 12,
    fontFamily: 'Regular',
    color: '#808080',
  },
});
