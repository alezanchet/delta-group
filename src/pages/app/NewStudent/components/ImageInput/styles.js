import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    marginBottom: 16,
  },

  image: {
    height: 90,
    width: 90,
    borderRadius: 30,
    backgroundColor: '#cccccc',
  },

  text: {
    color: '#f2f2f2',
    marginVertical: 8,
    fontFamily: 'Regular',
  },
});
